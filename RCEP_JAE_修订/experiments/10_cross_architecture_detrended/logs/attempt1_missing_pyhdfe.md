# Failed execution attempt 1

The first execution stopped before reading the analysis panel or estimating a
coefficient because the isolated environment did not include `pyhdfe`.
`pyhdfe==0.2.0` was added explicitly to `requirements.txt`; no PAP, sample,
variable, estimator, seed, or gate changed.
